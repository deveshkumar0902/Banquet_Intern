SMTP / Email Configuration (Mailtrap for development)
    SMTP_HOST=
    SMTP_PORT=
    SMTP_USER=
    SMTP_PASS=
    FROM_EMAIL=
    
    Server
    PORT=5000
    
    CORS (Frontend URL)
    CORS_ORIGIN=http://localhost:5173